models:
  - name: test_surrogate_key
    description: Validates that surrogate_key() produces unique, non-null keys per business key.
    columns:
      - name: customer_key
        tests:
          - unique
          - not_null
      - name: customer_id
        tests:
          - unique
          - not_null
